# Simple Linked List

* `Go` version: 1.18+

## Init
`go mod download`

## Running example
`go run main.go`

## Testing
`go test -v -failfast ./...`

## Benchmarking(?)
`go test -benchmem -run=^$ ./list -bench .`